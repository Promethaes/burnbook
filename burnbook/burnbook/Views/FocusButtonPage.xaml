<Page
    x:Class="burnbook.Views.FocusButtonPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock x:Name="CounterButtonText" TextWrapping="WrapWholeWords" HorizontalAlignment="Center" FontSize="40" Height="300"/>
        <TextBlock x:Name="DayNumber" FontSize="60" Height="550" HorizontalAlignment="Center"/>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Click="ClickCounter" Background="AliceBlue"  Padding="25">
                <TextBlock Text="🔥" FontSize="45"/>
            </Button>
        </StackPanel>
        <Button Content="📖 New Day 🌞" VerticalAlignment="Bottom" HorizontalAlignment="Center" FontSize="28" Click="NewDay" Padding="30"/>
    </Grid>
</Page>
